# cron job to fetch news every 30 mins

cron:
- description: "fetch news every 3 hours"
  url: /fetch-news
  schedule: every 3 hours
  target: getmachinanovanews
  retry_parameters:
    job_retry_limit: 2
    min_backoff_seconds: 2.5
    max_backoff_seconds: 5.0